<%- include ('./partials/head') %>

<main>

    <section>
        <h2>Vraag 5 van 7</h2>
        <div class="progress-bar"><div class="progress-v5"></div></div>
        <form method="post" action="/vraag6">
            <label><h2>Wanneer ben je geboren?</h2><input placeholder=" " type="text" name="verjaardag" required max="2002-01-01" pattern="(0[1-9]|1[0-9]|2[0-9]|3[01]).(0[1-9]|1[012]).[0-9]{4}">
                <p class="hint">DD-MM-YYYY</p></label>
            <!-- <input type="date" name="verjaardag" required max="2002-01-01">  -->

            <input type="hidden" name="userid" value=<%= userid %>>

            <!-- Navigation -->
            <nav>
                <button type="submit" value="vraag6">Verder</button>
            </nav>
        </form>
    </section>

    <%- include ('./partials/footer') %>